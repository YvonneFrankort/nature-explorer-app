<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Splash theme -->
    <style name="Theme.NatureGameSplash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/green_800</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.NatureGame</item>
    </style>

    <!-- Main app theme -->
    <style name="Theme.NatureGame" parent="Theme.AppCompat.DayNight.NoActionBar" />

</resources>
